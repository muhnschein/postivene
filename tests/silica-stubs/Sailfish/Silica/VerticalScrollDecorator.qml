import QtQuick 2.0

// Silica's scrollbar: a thin mark down the side of a flickable while it
// moves. It draws itself and takes no input, so there is nothing here
// for a test to look at -- it exists so a page that has one can be
// loaded.
Item {
}
